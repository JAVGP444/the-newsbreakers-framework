# Linux / Linux

[Español](#español) · [English](#english)

---

## Español

```bash
chmod +x linux/abrir.sh linux/detener.sh
./linux/abrir.sh
```

Para parar: `./linux/detener.sh`.

Empaquetado: `python packaging/build.py` → `dist/NewsBreakers/` + `.desktop`.

Idioma de la app: botón **ES | EN** en la cabecera.

---

## English

```bash
chmod +x linux/abrir.sh linux/detener.sh
./linux/abrir.sh
```

To stop: `./linux/detener.sh`.

Packaging: `python packaging/build.py` → `dist/NewsBreakers/` + `.desktop`.

App language: **ES | EN** button in the header.
